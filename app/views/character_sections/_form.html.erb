<%= form_for(character_section) do |f| %>
  <% if character_section.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(character_section.errors.count, "error") %> prohibited this character_section from being saved:</h2>

      <ul>
      <% character_section.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :section_id %>
    <%= f.text_field :section_id %>
  </div>

  <div class="field">
    <%= f.label :character_id %>
    <%= f.text_field :character_id %>
  </div>

  <div class="field">
    <%= f.label :value %>
    <%= f.text_area :value %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
